For the University dataset: 
  The best configuration of hyperparameters for the CourseDREAM model:
  number of LSTM layers = 3, embedding dimension = 64, dropout = 0.4, learning rate = 0.01, number of epochs = 10.

  The best configuration of hyperparameters for the ScholarsWalk model:
  previous course to start a random walk = any of the last semester's courses taken by that student.

  The best configuration of hyperparameters for the PlanBERT model:
  percentage of masked tokens = 25%.

  The best configuration of hyperparameters for the matrix factorization models:
  NMF model --> latent dimension = 15, number of epochs = 10
  PMF model --> latent dimension = 20, number of epochs = 15
  LightFM+BPR model --> latent dimension (factors) = 10, number of epochs = 20
  LightFM+WARP model --> latent dimension = 10, number of epochs = 30

  The best configuration of hyperparameters for the KEAM model:
  number of neighbors = 200.

  The best configuration of hyperparameters for the UPGPR model:
  maximum path length = 7, embedding dimension = 64, number of hidden units= 128, 64, number of epochs = 100.

For the MOOCCube dataset: 


For the CNPC dataset: 

  
